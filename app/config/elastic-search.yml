fos_elastica:
    clients:
        default: { host: "%elastic_host%", port: "%elastic_port%" }
    indexes:
        books:
            index_name: books
            types:
                book:
                    mappings:
                        title: ~
                    persistence:
                        driver: orm
                        model: AppBundle\Entity\Book
                        provider: ~
                        listener: ~
                        finder: ~