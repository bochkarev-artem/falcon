{% if pagination.total_pages > 1 %}
    {% if view is defined and view != 'column' %}
        {% set url_parameters = '?view=' ~ view %}
    {% else %}
        {% set url_parameters = '' %}
    {% endif %}
    <!--KODDE PAGINATION START-->
    <div class="col-md-12">
        <div class="kf-pagination">
            <ul class="pagination">
                <li>
                    {% if pagination.current != 1 %}
                        <a href="{{ url_page ~ pagination.prev ~ url_parameters }}" aria-label="{{ 'front.pagination.previous'|trans }}">
                            <span aria-hidden="true">
                                <i class="fa fa-angle-left"></i>
                            </span>
                            <small class="page-label">
                                {{ 'front.pagination.previous'|trans }}
                            </small>
                        </a>
                    {% endif %}
                </li>

                {% for page in pagination.pages %}
                    <li>
                        <a{% if page == pagination.current %} class="active"{% endif %}
                                href="{{ page != pagination.current ? url_page ~ page  ~ url_parameters : '' }}">
                            {{ page }}
                        </a>
                    </li>
                {% endfor %}

                <li>
                    {% if pagination.current != pagination.last %}
                        <a href="{{ url_page ~ pagination.next ~ url_parameters  }}" aria-label="{{ 'front.pagination.next'|trans }}">
                            <span aria-hidden="true">
                                <i class="fa fa-angle-right"></i>
                            </span>
                            <small class="page-label">
                                {{ 'front.pagination.next'|trans }}
                            </small>
                        </a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
    <!--KODDE PAGINATION END-->
{% endif %}